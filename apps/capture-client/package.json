{
  "name": "capture-client",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esbuild src/ui/main.ts --bundle --sourcemap --outdir=dist --servedir=. --serve=0.0.0.0:8000",
    "build": "esbuild src/ui/main.ts --bundle --outdir=dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "vitest": "^1.4.0",
    "typescript": "^5.3.3"
  }
}
